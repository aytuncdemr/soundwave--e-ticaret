import SignForm from "@/components/SignForm";

export default function LoginPage() {
    return (
        <section className="login-section flex items-center min-h-screen justify-center">
            <SignForm></SignForm>
        </section>
    );
}
